{% extends 'base.html.twig' %}

{% block title %}{{ produit.nom }}{% endblock %}

{% block body %}
    <div class="row" style="margin-top:30px" > 
        <div class="col-md-5">
            <img class="img-fluid" src="/uploads/images/{{produit.image}}" alt="{{produit.nom}}">
        </div>
        <div class="col-md-7 my-auto">
            <h3>{{produit.nom}}</h3>
           
            <span class="product-price">{{(produit.pu  )|number_format(2,',', '.')}} € </span>
            <hr>
            <p>{{produit.description}}</p>
        <a class="btn btn-outline-success">Ajouter au panier</a>
      {% if is_granted('ROLE_ADMIN') %}  <a href="{{ path('Edit_Produit', {id: produit.id}) }}"class="btn btn-outline-success">
            Modifier Produit
        </a>
        {% endif %}

        </div>
    </div>   
{% endblock %}
